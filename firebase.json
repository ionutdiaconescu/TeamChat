{
  "hosting": {
    "public": "dist",
    "ignore": ["firebase.json", "**/.*", "**/node_modules/**"]
    // no rewrites: we host multiple static pages under /src/pages,
    // so requests should resolve directly to the corresponding folder
    // instead of always falling back to index.html.
  }
}
